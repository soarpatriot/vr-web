version: '3'
services: 
  webui: 
    build: docker
    image: soar/webui:1.0.0
    container_name: webui
    volumes: 
      - <%= fetch(:deploy_to) %>:/data/www/vr-web
    command: bash -c "cd current && npm install && npm run build"

